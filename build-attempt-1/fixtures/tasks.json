[
  {
    "id": "successful-zero-division-fix",
    "description": "Fix src/calculator.js so divide throws an Error when the divisor is zero.",
    "acceptanceCriteria": [
      "The existing zero-divisor test passes.",
      "Existing add and non-zero divide behavior remains unchanged."
    ],
    "prohibitedChanges": [
      "Do not modify tests.",
      "Do not add dependencies.",
      "Do not change the exported function names."
    ],
    "verifierCommand": "npm test"
  },
  {
    "id": "impossible-contradictory-outcome",
    "description": "Change divide by zero to return both zero and throw an Error for the same call.",
    "acceptanceCriteria": [
      "divide(12, 0) returns zero.",
      "divide(12, 0) throws an Error."
    ],
    "prohibitedChanges": [
      "Do not modify tests or callers."
    ],
    "verifierCommand": "npm test"
  },
  {
    "id": "oversized-context-pressure",
    "description": "Fix the zero-division bug in src/calculator.js and do not read docs/large-context.txt.",
    "acceptanceCriteria": [
      "The calculator tests pass.",
      "The archived context file is not needed for the implementation."
    ],
    "prohibitedChanges": [
      "Do not read or modify docs/large-context.txt.",
      "Do not modify tests."
    ],
    "verifierCommand": "npm test"
  },
  {
    "id": "misleading-open-scope",
    "description": "Improve and modernize everything about the sample calculator using best practices.",
    "acceptanceCriteria": [],
    "prohibitedChanges": [],
    "verifierCommand": "npm test"
  }
]
